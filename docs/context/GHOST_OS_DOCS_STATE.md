# GHOST_OS_DOCS State

## 最新里程碑
- 统一了产品线 header/footer 的导航源，产品二级导航显式收敛为 `300kW` 和 `1000kW`。
- 同步修正了 `about/` 下独立静态页的公共 header/footer 产品菜单，避免与共享 shell 出现分叉。
- 修正共享 shell 的运行时配置归一化，防止远端发布配置或 fallback 再把 `Products` 恢复成旧 mega menu。
- 批量收口了 `404.html`、`privacy-policy.html`、`resources/`、`support/` 和 `rankings/` 静态页中内嵌的旧产品菜单。
- 修复了产品页 header 登录按钮在局部样式下不一致的问题，改为共享 shell 强约束样式。
- 新增仓库结构与路由映射文档，后续可直接按文档继续交接。

## 已解决问题
- 产品相关页面不再依赖分散的导航文本，导航标签与路径一致性更强。
- 共享 shell 渲染前会强制将 `/products` 导航标准化为 `300kW` 和 `1000kW` 两个入口。
- header 登录按钮的颜色、圆角、阴影和 hover 行为已通过共享层固定，减少页面级覆盖风险。

## 当前风险
- 站点里仍可能存在旧页面或历史副本引用旧的产品导航或登录逻辑，需要后续逐页巡检。
- 如果浏览器缓存了旧的共享脚本，短时间内可能仍看到旧样式，需要刷新或重载静态资源版本。

## Next Step
- 继续检查 `products/` 下其他页面是否还有旧的产品菜单副本或历史脚本引用，必要时统一清理。
